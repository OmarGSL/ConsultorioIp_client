export const loginReducer = ({ dispach, payload }) => {
    switch (dispach) {
        case 'login':
            break;
        case 'register':
            break;
        default:
            break;
    }
}
